
// Online C Compiler - Build, Compile and Run your C programs online in your favorite browser

#include<stdio.h>

int main()
{
    /* Utilizamos o comando scanf() para realizar a leitura de dados para uma variável. Sua estrutura
    baseia-se da seguinte forma: scanf("<tipo do dado>",&variável).
    Ex: scanf("%f",&x)
    O tipo de dado é a expressão de controle, ou seja, a expressão de controle vai definir o tipo de dado
    que vai ser scaneado e jogado para a variável X...*/
    
    // Tipos de dados de controle
    /*
    int - %d
    long int - %ld 
    char - %c 
    float - %f 
    long float - %lf 
    */
    
    /* Quando estamos manipulando esse scanf em uma variável, sua sintaxe significa a seguinte coisa:
    Leia um dado de tipo Float e jogue para o endereço de X (&x - Esse E comercial nada mais é que um
    identificador do endereço da memória da variável).
    
    Para estruturar o scanf, podemos colocar dentro das aspas vários identificador de controle para puxar
    e armazenar os dados, mas devido a organização, podemos separar por printf.
    */
    
    return 0;
}